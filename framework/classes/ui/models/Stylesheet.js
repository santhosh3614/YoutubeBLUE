//= require Observer
//= require ui.models.ComponentModel


namespace('ui.models.Stylesheet', {
	'@inherits': ui.models.DataModel,

	initialize : function(options, element){
		/*this.data = options.data;
	    this.pageSize = options.pageSize;
	    this.currentPage=0;*/
	   	//this.parent(options);
		
	}
});
